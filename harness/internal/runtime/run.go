package runtime

// DefaultStorePath is the canonical Local Mnemon kernel-store path under the
// project root. The daemon, CLI, and setup all resolve the store here.
const DefaultStorePath = ".mnemon/harness/local/governed.db"
